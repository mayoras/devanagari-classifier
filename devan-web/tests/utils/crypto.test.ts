/* eslint-disable @typescript-eslint/no-non-null-assertion */
import { describe, expect, it, beforeEach, assert, test } from 'vitest';
import { identifyPayloadImages } from '$lib/utils/crypto';

type PayloadImageProps = devan.image.PayloadImageProps;

describe('identifyPayloadImages', () => {
	let payloads: PayloadImageProps[];

	beforeEach(() => {});

	it('should be a function', () => {});
	it('should return IDs for each payload image', () => {});
	test('IDs are unique for each image', () => {});
	it('should return null if no images are given', () => {});
});
